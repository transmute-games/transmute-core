# Transmute Core (TypeScript)

Typed agent-subset port. Same contracts as Java / Python / JS / C.

```bash
npm install
npm test
cd ../../examples/typescript/hello && npm install && npm run verify
```

See [contracts/](../../contracts/).
