/**
 * PNG-indexed tile levels and related types.
 * <p>
 * <b>Prefer {@link TransmuteCore.world.World}</b> for new tile games (declarative via
 * {@code GameSpec} {@code world.*} / {@code spawn.*} / {@code trigger.*} keys).
 * {@link TransmuteCore.level.TiledLevel} and {@link TransmuteCore.level.Level} are
 * {@code @Deprecated}; kept for existing image-based level projects.
 */
package TransmuteCore.level;
