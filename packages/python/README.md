# Transmute Core (Python)

Contract-compatible port of the agent subset: GameSpec, World, Body2D, headless verify.

See [contracts/](../../contracts/) and [docs/AUTHORING.md](../../docs/AUTHORING.md).

```bash
pip install -e ".[dev]"
pytest
cd ../../examples/python/hello && python -m hello --headless
```
