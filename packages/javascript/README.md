# Transmute Core (JavaScript / Node)

Contract-compatible agent subset. See [contracts/](../../contracts/).

```bash
npm install
npm test
cd ../../examples/javascript/hello && npm install && npm run verify
```
